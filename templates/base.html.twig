<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Eclairage - Gestion{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    {# Bootstrap CSS #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    {% block stylesheets %}{% endblock %}
</head>
<body class="bg-light">

<header class="navbar navbar-expand-lg navbar-dark bg-dark px-3">
    <a class="navbar-brand text-white" href="#">Eclairage</a>
    {% block header %}{% endblock %}
    {% if is_granted('ROLE_ADMIN') %}
        <a href="{{ path('admin_dashboard') }}" class="btn btn-outline-primary mb-3">← Retour au tableau de bord admin</a>
    {% elseif is_granted('ROLE_USER') %}
        <a href="{{ path('user_dashboard') }}" class="btn btn-outline-primary mb-3">← Retour au tableau de bord vendeuse</a>
    {% endif %}
</header>

<main class="container py-4">
    {% block body %}{% endblock %}
</main>

<footer class="text-center text-muted py-3">
    <small>&copy; {{ "now"|date("Y") }} Eclairage - Tous droits réservés</small>
</footer>

{# Bootstrap JS + Optionnel: JS centralisé #}
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script src="{{ asset('assets/js/app.js') }}"></script>

{% block javascripts %}{% endblock %}
</body>
</html>
