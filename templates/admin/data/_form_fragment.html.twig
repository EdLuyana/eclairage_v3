{{ form_start(form, {'action': path('admin_data_manage', {'type': type}) }) }}
    {{ form_widget(form) }}
<button class="btn btn-success mt-2 w-100">Ajouter {{ label|lower }}</button>
{{ form_end(form) }}
