{% extends 'base.html.twig' %}

{% block title %}Réassort{% endblock %}

{% block body %}
    <h1 class="mb-4">📦 Réassort produit</h1>

    {% for message in app.flashes('danger') %}
        <div class="alert alert-danger">{{ message }}</div>
    {% endfor %}

    <div class="card p-4 shadow-sm mb-4">
        <form method="get" action="{{ path('admin_reassort_start') }}">
            <label for="reference" class="form-label">Référence produit</label>
            <div class="input-group">
                <input type="text" id="reference" name="reference" class="form-control" placeholder="ex : Zara_Ete2025_Robe_bleue" value="{{ reference }}">
                <button class="btn btn-primary" type="submit">Rechercher</button>
            </div>
        </form>
    </div>

    {% if product %}
        <div class="card p-4 shadow-sm">
            <h5 class="mb-3">Produit trouvé :</h5>
            <ul>
                <li><strong>Nom :</strong> {{ product.name }}</li>
                <li><strong>Couleur :</strong> {{ product.color }}</li>
                <li><strong>Collection :</strong> {{ product.season.name }}</li>
                <li><strong>Fournisseur :</strong> {{ product.supplier.name }}</li>
            </ul>

            <hr>

            <h5 class="mt-4">Quantité par taille à redistribuer</h5>
            <form method="post" action="#">
                <div class="row g-2">
                    {% for size in ['XS', 'S', 'M', 'L', 'XL', '36', '38', '40', '42'] %}
                        <div class="col-4 col-md-2">
                            <label class="form-label">{{ size }}</label>
                            <input type="number" name="sizes[{{ size }}]" min="0" class="form-control" value="0">
                        </div>
                    {% endfor %}
                </div>
                <button class="btn btn-success mt-3">Suivant : répartir par magasin</button>
            </form>
        </div>
    {% endif %}
{% endblock %}
