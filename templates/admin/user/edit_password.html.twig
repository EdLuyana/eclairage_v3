{% extends 'base.html.twig' %}

{% block title %}Modifier le mot de passe{% endblock %}

{% block body %}
    <h1 class="mb-4">🔑 Modifier le mot de passe</h1>

    <p class="text-muted">Compte concerné : <strong>{{ user.username }}</strong></p>

    {{ form_start(form) }}
    <div class="mb-3">
        {{ form_label(form.plainPassword, 'Nouveau mot de passe') }}
        {{ form_widget(form.plainPassword, {'attr': {'class': 'form-control'}}) }}
        {{ form_errors(form.plainPassword) }}
    </div>

    <button class="btn btn-primary">💾 Enregistrer</button>
    <a href="{{ path('admin_user_index') }}" class="btn btn-outline-secondary ms-2">↩ Retour</a>
    {{ form_end(form) }}
{% endblock %}
