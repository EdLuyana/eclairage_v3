<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Étiquettes produits</title>
    <style>
        @page {
            margin: 0;
        }

        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            font-size: 7px;
        }

        table {
            width: 210mm;
            height: 297mm;
            border-collapse: collapse;
            table-layout: fixed;
        }

        td {
            width: 52.5mm;
            height: 21.2mm;
            padding: 1mm;
            box-sizing: border-box;
            vertical-align: middle;
            text-align: center;
        }

        .ref {
            font-size: 6px;
        }

        .barcode {
            margin: 1mm 0;
        }

        .size {
            font-size: 8px;
            margin-top: 1mm;
        }

        .price {
            font-weight: bold;
            font-size: 9px;
        }

        .magasin {
            font-size: 10px;
            font-weight: bold;
            background: #f5f5f5;
        }
    </style>
</head>
<body>
<table>
    <tbody>
    {% for locationName, labels in groupedLabels %}
        <tr>
            <td class="magasin">📍 MAGASIN {{ locationName|upper }}</td>
            <td></td><td></td><td></td>
        </tr>

        {% set counter = 0 %}
        <tr>
        {% for label in labels %}
            <td>
                <div class="ref">{{ label.reference }}</div>
                <div class="barcode">
                    <img src="data:image/png;base64,{{ label.barcode }}" style="width: 45mm; height: 14mm;" />
                </div>
                <div class="size">{{ label.size }}</div>
                <div class="price">{{ label.price }}</div>
            </td>

            {% set counter = counter + 1 %}
            {% if counter % 4 == 0 %}
                </tr><tr>
            {% endif %}
        {% endfor %}

        {# Remplir les colonnes restantes #}
        {% for i in 1..(4 - (counter % 4)) %}
            {% if counter % 4 != 0 %}
                <td></td>
            {% endif %}
        {% endfor %}
        </tr>
    {% endfor %}
    </tbody>
</table>
</body>
</html>
